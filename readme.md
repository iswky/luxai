## Place any .gguf (gemma4 is preff) model into llm-service/models/model.gguf

## Запуск только web-интерфейса
Запуск из корневой директории проекта:
```bash
python3 ./backend/manage.py runserver
```
